import { type NextPage } from "next";
import { MiniHeader } from "~/components/miniHeader";

const Home: NextPage = () => {
  return (
    <div>
      <MiniHeader />
    </div>
  );
};

export default Home;
